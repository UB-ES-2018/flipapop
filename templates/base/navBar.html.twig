
<nav class="navbar navbar-expand-lg fixed-top justify-content-between">
    <a class="navbar-brand" href="{{ path('landing_page') }}">Flipapop</a>

    <!-- Search form -->
    <form class="form-inline md-form form-sm active-cyan-2" method="post" action="{{ path('landing_page') }}">
        <input class="form-control form-control-sm mr-3" style="width: 300px" name="searchbar" type="text" placeholder="Search Products" aria-label="Search">
        <i class="fa fa-search" aria-hidden="true"></i>
    </form>
    {% if app.user is not null %}
        <a class="btn btn-primary" role="button" href="{{ path('new_product') }}">
            Upload product
        </a>
    {% endif %}
    {% if app.user is null %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('login') }}">Sign in</a>
            </li>
            <span class="navbar-text"> or</span>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('register_user') }}">Sign up</a>
            </li>
        </ul>

    {% else %}
        <a class="btn btn-primary" role="button" href="{{ path('user_profile') }}">{{ app.user.name }}</a>
    {% endif %}
</nav>